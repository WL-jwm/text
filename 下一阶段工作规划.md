# 河北省地下水环境信息平台 — 下一阶段工作规划

> 基准版本: v5.0.0 | 2026-07-08
> Phase 5 质量加固全部完成（ESLint 0/0, tsc 0, vitest 307, CI OK）

---

## 一、当前平台盘点

### 1.1 代码规模

| 维度 | 数值 |
|------|------|
| 页面 | 30 个（28 导航 + saline-water + NotFound） |
| 通用组件 | 37 个顶层 + 98 个子组件 |
| 数据模块 | 41 个 |
| Hooks | 7 个（usePageCommons / useReportData / useServiceWorker / useStoreInit / useScrollMemory / useMobile / useChartInteraction） |
| 状态管理 | 2 个 Zustand store（useAppStore / reportCacheStore） |
| 报告生成器 | 28 个（每页面一个 Word/Excel 报告） |
| 测试 | 17 个测试文件 / 307 个测试用例 |
| 总代码行 | ~52,350 行 |

### 1.2 质量指标

| 指标 | 状态 |
|------|------|
| ESLint | 0 errors / 0 warnings |
| TypeScript | 0 编译错误 |
| Vitest | 307 tests passed（0 failures） |
| Vite build | 通过 |
| CI 脚本 | `npm run ci`（lint + tsc + test） |
| Git | 5 commits（init / Phase 5a-c / Phase 5d） |

---

## 二、已完成工作总览

### Phase 4d：代码质量治理（v4.10.0）

| 批次 | 内容 |
|------|------|
| Phase 4a-b | tsc 预存错误修复、CrossLink 重复键修复、ResourcesBulletinTab 属性修复 |
| Phase 4c | ESLint 集成（eslint@9 + typescript-eslint + react-hooks + react-refresh） |
| Phase 4d | 代码质量治理：未使用 import/变量清理、hooks 条件调用修复、ESLint 配置优化 |

### Phase 5：质量加固（v5.0.0）

| 子阶段 | 内容 | 测试变化 |
|--------|------|---------|
| 5a 测试覆盖扩展 | 新增 7 个数据模块测试（zoneParams/standards/mapData/hydroParams/geology/backgroundValues/dataSourceRegistry） | 105 → 261 |
| 5b 路由集成测试 | 新增 routes.test.ts（28 路由定义 / 导航↔路由双向一致性 / 页面文件存在性 / 格式规范） | 261 → 277 |
| 5c Store 单元测试 | 新增 reportCacheStore.test.ts（simpleHash/buildCacheKey/CRUD/过期/invalidate/getStats），修复 2 个历史超时 | 277 → 307 |
| 5d P0/P1 修复 + 工程治理 | 版本号动态化 / API 环境变量化 / 生产 console 移除 / README / vite-env.d.ts / param-service 安全修复 | 307（不变） |

### Phase 5d 审查报告处理

> 参考：深度代码审查报告（20 个问题点）

| 编号 | 问题 | 定级 | 处理 |
|------|------|------|------|
| 2.1 | Flask debug=True + 0.0.0.0 | P0 | **已修复**：环境变量控制，默认 127.0.0.1 + debug=false |
| 2.2 | CORS `*` + 零认证 | P0 | **已修复**：ALLOWED_ORIGINS 白名单 |
| 2.3 | 两套后端共存 | P0 | **澄清**：前端仅调用 Flask GET 端点，POST/DELETE 是 FastAPI 特有，前端未使用 |
| 3.1 | dangerouslySetInnerHTML | P1 | **此前已修复**（代码中不存在） |
| 3.2 | window.__navigate hack | P1 | **此前已修复**（代码中不存在） |
| 3.3 | 版本号不一致 | P1 | **已修复**（动态读取 package.json + TOTAL_PAGES） |
| 3.4 | 测试覆盖失衡 | P1 | **持续改善**（105 → 307） |
| 3.5 | .tmp 文件残留 | P1 | **此前已清理**（文件系统中不存在） |
| — | API_BASE 硬编码 | P2 | **已修复**（VITE_API_BASE 环境变量） |
| — | console 调用 | P2 | **已修复**（esbuild pure 移除 log/info/warn/debug） |
| — | 无 README | P2 | **已修复** |
| — | 报告生成器超时 | P2 | **已修复**（timeout 20s/30s） |
| 4.2 | TypeScript as any（49 处） | P2 | **留待 Phase 6a** |
| 4.3 | 大文件未拆分（MapView/TimeSeries/Overview） | P2 | **留待 Phase 6b** |
| 4.5 | 无 vite-env.d.ts | — | **已修复** |
| 5.1 | 状态管理分裂 | P2 | **留待后续** |
| 4.1 / 4.4 / 5.3 | GW-Predict 相关 | P2 | **超出范围** |

---

## 三、技术债务清单（更新）

### 3.1 中优先级

| 编号 | 问题 | 范围 | 建议 |
|------|------|------|------|
| D1 | TypeScript as any（49 处） | 全项目 | 分批添加类型守卫，优先 public 接口 |
| D2 | 大文件未拆分 | MapView(1578L) / TimeSeries(1567L) / Overview(1490L) | 每个拆为 3-5 子组件，主文件 < 500L |
| D3 | Overview 同步导入 20+ 模块 | App.tsx | 依赖 D2（大文件拆分后自然懒加载） |
| D4 | usePageCommons hook 未启用 | 11 个页面 | 迁移 exportOpen/getData/useToast 到 hook，消除模板重复 |
| D5 | Zustand useAppStore 使用率低 | 仅 5 个文件引用 | 审计后决定保留/补充/移除 |

### 3.2 低优先级

| 编号 | 问题 | 范围 | 建议 |
|------|------|------|------|
| D6 | plain Tooltip 残留 | KarstWater / MineralWater / Resources | 统一为 ChartTooltip |
| D7 | 状态管理分裂（Zustand/模块级/window） | 全项目 | 渐进统一到 Zustand |
| D8 | param-service 未纳入 Git 版本控制 | param-service/ | 创建 monorepo 或子模块 |

---

## 四、下一阶段工作路线

### Phase 6a：类型安全加固（v6.0.0-pre）

> 目标：减少 as any 使用，提升 TypeScript 类型覆盖率

| 任务 | 范围 | 说明 |
|------|------|------|
| as any 审计与分类 | 全项目 49 处 | 按文件分组，标记每处可修复性（可直接类型断言 / 需要接口定义 / 需要重构） |
| public 接口类型补全 | paramService.ts / stores / hooks | 为 API 返回值、store state、hook 参数添加精确类型 |
| 类型守卫函数 | services/ | 为 union type 添加 `isXxx()` 类型守卫，替代 `as any` |

**预期**: as any 从 49 → < 15，类型覆盖率显著提升

### Phase 6b：大页面拆分

> 目标：降低单文件复杂度，提升可维护性

| 文件 | 当前行数 | 拆分方案 |
|------|----------|---------|
| MapView.tsx | 1578 | MapView + MapLayerPanel + MapLegend + MapMarkerPopup + useMapData |
| TimeSeriesAnalysis.tsx | 1567 | TimeSeriesAnalysis + TrendChart + ComparisonChart + useTimeSeriesData |
| Overview.tsx | 1490 | Overview + KpiGrid + WaterSupplyChart + QuickLinks + useOverviewData |

**预期**: 最大文件 < 500 行，每个页面 3-5 个子组件

### Phase 6c：组件测试（jsdom 环境）

> 前置条件：安装 @happy-dom（替代 jsdom，避免 Windows native addon 锁定问题）

| 任务 | 范围 | 说明 |
|------|------|------|
| 安装 @happy-dom | vitest.config.ts | `environment: 'happy-dom'`，创建 setup 文件 |
| StatCard 渲染测试 | components/ | props → title / value / trend / icon 正确渲染 |
| FilterableTechTable 交互测试 | components/ | 搜索过滤 / 列排序 / 分页 |
| ChartTooltip 测试 | components/ | 鼠标悬停 → 正确显示 tooltip |
| 通用组件快照测试 | 6 个组件 | 确保结构不意外变化 |

**预期**: 测试 307 → 400+，组件层首次有测试覆盖

### Phase 6d：沉睡数据唤醒

> 目标：已录入但未可视化的数据产生价值

| 页面 | 沉睡数据 | 可新增可视化 |
|------|----------|-------------|
| Environment | shallowCones2024 / deepCones2024 | 各漏斗中心埋深/面积堆叠图 |
| Exploitation | cityOverExploitDetail | 各市超采量排名变化 |
| SalineSoil | salineSoilTypes / salineSoilGenesis | 成因分类饼图 + 案例卡片 |
| MineHydrogeology | mineDrainageImpact / mineWaterInrushHistory | 矿坑涌水量趋势 |
| SalineWater | salineTypes / salineUtilization | 矿化度分级分布 |

**预期**: 平台内容密度提升 30%+

---

## 五、里程碑

| 里程碑 | 阶段 | 版本 | 核心产出 |
|--------|------|------|---------|
| M1: 质量加固 | Phase 5 | **v5.0.0（已完成）** | 测试 307 / CI / README / 安全修复 |
| M2: 类型安全 | Phase 6a | v6.0.0-pre | as any < 15 |
| M3: 架构优化 | Phase 6b | v6.0.0 | 最大文件 < 500L |
| M4: 组件测试 | Phase 6c | v6.1.0 | 测试 400+，组件层覆盖 |
| M5: 数据深挖 | Phase 6d | v6.2.0 | 5 页面沉睡数据可视化 |
| M6: 交付就绪 | Phase 7 | v7.0.0 | 响应式 / 无障碍 / 暗色模式 / 性能报告 |

---

## 六、优先级建议

| 优先级 | 阶段 | 理由 |
|--------|------|------|
| **1（立即执行）** | **Phase 6a 类型安全加固** | as any 是类型系统最大漏洞，修复成本低收益高 |
| **2（短期）** | **Phase 6b 大页面拆分** | 降低维护成本，为大文件懒加载（D3）铺路 |
| **3（短期）** | **Phase 6c 组件测试** | 安装 @happy-dom，首次覆盖组件层 |
| 4 | Phase 6d 沉睡数据唤醒 | 已录入数据价值最大化 |
| 5 | Phase 7 交付就绪 | 面向正式部署 |
