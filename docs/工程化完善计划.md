# 工程化完善实施计划（方向 A）

> 日期: 2026-08-16 | 质量基线: tsc 0 + ESLint 0 + Vitest 1101 + Playwright E2E

## 一、审计基线（2026-08-16 实测）

| 维度 | 实测值 | 说明 |
|------|--------|------|
| 最大源码文件 | dataValidation.ts 2096 行 | as any 集中地 |
| 次大文件 | WellNetworkPanel.tsx 1938 / NumericalFlowSimulatorTab 1117 / remediationEvaluator 1030 / numericalFlowSimulator 1016 / realtimeDataSource 1005 / BalanceCalculatorTab 925 / MapView 903 / resources-bulletin 866 / Overview 862 | — |
| 生产代码 as any | ~40 处（含测试 56 处） | dataValidation.ts ~30 / wellReportPdf.ts 8 / MapView 1 / alertNotifier 1 |
| usePageCommons 使用 | 18 个文件引用 | D4 已基本落地 |
| E2E 测试 | 仅 app.spec.ts（3 链路） | 需扩展 |
| param-service | 位于工作区根，在 frontend 仓库之外 | D8 未纳入 Git |
| README 测试数字 | 仍为 307（17 文件） | 已过时，实际 1101 |

## 二、任务分解

### E-01 README 更新（低风险，立即执行）
- 测试命令 `307 个测试用例 (17 文件)` → `1101` + Playwright E2E
- 补充 E2E 运行说明、playwright.config 说明

### E-02 类型安全修复（D1 + 全量 tsc 归零）

> **2026-08-16 重要发现**：README 声称 tsc 0，但实际 `tsc -b -noEmit` 有 **116 个类型错误**（基线即存在，非本次引入）。根因是类型定义（WellReportData/WellAlert/Well 等）与实际数据模型脱节、hooks 模块引用、测试 mock 与类型不符。E-02 目标由"仅消除 as any"升级为"修复全部类型错误使 tsc 归零"。

错误分布（116 个）：
- 报告生成器 wellReportPdf(20)/wellReportExcel(13)/wellReportDocx(7) = 40 → WellReportData 等类型缺字段
- dataExporter(11)/dataImporter(2) = 13
- alertNotifier(9) → WellAlert 缺字段
- hooks useWaterQualityBalance(3)/useWaterQuality(2)/useWaterBalance(2) = 7 → import 模块缺失
- WellNetworkPanel(5)
- 测试文件：spatialStatsCalculator(20)/timeSeriesCalculator(5)/balanceCalculator(3)/dataExporter(5)/spatialOptimizer(4)/alertNotifier(3)/ErrorBoundary(2) = 42 → mock 与类型不符

已完成：
- wellReportPdf.ts jsPDF 类型（新增 src/types/jspdf-autotable.d.ts，消除 doc 相关 as any）
- MapView.tsx contourLayerRef → LImageOverlay
- alertNotifier.ts webkitAudioContext → 局部类型断言

进度（E-02 已完成，tsc 116→0，测试 1101/1101 全绿）：
- ✅ 修复 hooks 模块引用（./wellNetwork → ../services/wellNetwork）
- ✅ 统一报告数据模型：wellReport.ts 同时产出扁平(alerts/cities/aquifers/realtime，供 Pdf/Excel)与 xxxRows(供 Docx)两套，补齐 meta.reportId、summary.alertCount/criticalAlerts/assessmentDate；修复 Pdf/Excel 报告运行时 data.alerts undefined 的缺陷
- ✅ dataExporter/alertNotifier/dataImporter 适配核心 Well/WellAlert 模型
- ✅ 测试 mock 对齐（spatialStats/timeSeries/balance/dataExporter/spatialOptimizer/alertNotifier）
- ✅ 补全 jsPDF autoTable/getNumberOfPages 类型、exceljs ConditionalFormatting priority、lucide TriangleAlert
- ✅ 修复 spatialStats pValue 浮点越界（clamp [0,1]）

**E-03 ESLint 归零（2026-08-16 完成）**：
- 33 个错误全部修复：unused-vars 20 / no-explicit-any 7 / no-dupe-else-if 2 / no-unused-expressions 4
- 关键修复：wellReportExcel/Pdf 的 ws:any→ExcelJS.Worksheet、data:any→CellHookData；WellNetworkPanel previewRows:any→ImportPreviewRow；修复 waterQuality pH no-dupe-else-if（重复分支 Ⅰ/Ⅱ/Ⅲ 类同为 6.5-8.5 不可达）
- eslint.config.js 新增 no-unused-expressions allowShortCircuit/allowTernary（合法 JSX 短路渲染）

**E-02+E-03 最终基线（2026-08-16 实测）**：tsc 0 + ESLint 0 + Vitest 1101/1101 + build 成功（20s）

### E-03 大文件拆分（D2/D3）—— dataValidation.ts 完成

> **2026-08-16 dataValidation.ts 拆分完成**：主文件 2096 → 450 行，46 个交叉校验函数按主题域拆到 `src/data/validators/` 8 个子文件：
> waterResources(230)/groundwaterLevel(215)/geology(281)/quality(193)/geothermal(208)/subsidence(145)/business(178)/common(61)
> 主文件保留 import + 类型 + scanArrayModule + scanAllModules + runAllValidations + 导出。
> 验证：tsc 0 + ESLint 0 + Vitest 1101/1101 全绿（无回归）。

**Overview.tsx 懒加载优化（2026-08-17 完成）**：
- 折叠面板内 9 个区块组件（HydrogeologyReferenceLibrary/ExploitationControlComparison/ExploitationManagement/AlluvialFansWaterSources/StandardsReferencePanel/HydroParamsReferencePanel/ZoneParamsPanel/HistoricalEvolution/PollutionQualityComparison）改为 React.lazy + Suspense
- CollapsiblePanel 折叠时不渲染 children，故懒加载真正延迟到展开时加载
- build 验证：9 个区块拆成独立 chunk（各 1-3KB）从主 bundle 分离；tsc 0 + ESLint 0 + Vitest 1101/1101

**WellNetworkPanel.tsx 子组件提取（2026-08-17 完成）**：
- 提取 6 个内联子组件到独立文件：WellMap/StatCard/RealtimeStatusBadge/WellTable/TrendSparkline/AddWellForm
- 新增共享常量 constants.ts（CHANNEL_LABELS/AQUIFER_COLORS/AQUIFER_TYPES/STATUSES/CHANNELS/RT_STATUSES）
- 主文件 1939 → 1574 行；验证 tsc 0 + ESLint 0 + Vitest 1101/1101 + build 成功
- 备份：WellNetworkPanel.tsx.bak_split

待办（可后续推进）：
- WellNetworkPanel 主组件巨型 JSX（~1500行）内部功能块提取（依赖共享状态，风险高）
- NumericalFlowSimulatorTab.tsx (1117) 拆子组件
- MapView.tsx 拆

### E-04 E2E 扩展（中价值）
- 从 3 链路扩展到核心页面（水质/水量平衡/数值模拟/数据导入）冒烟

### E-05 param-service 纳入管理（D8，需确认）
- 位于工作区根，不在 frontend 仓库。需确认：独立仓库 or monorepo or 仅本地保留

## 三、执行顺序建议
E-01 → E-02 → E-04 →（E-03 / E-05 需用户确认范围后推进）

每完成一项，跑 `npm run ci`（lint + tsc + test）+ 必要时 Playwright，确保基线不回退。
